schema_version: 1.0.0
id: uc-get-order-unit-test
use_case_id: uc-get-order
angle: unit-test
kind: assertion
nature: computational
output_type: single-shot

uses:
  - go-stdlib

steps:
  - id: probe
    run: "go run ./cmd/sensor-check --sensor=uc-get-order-unit-test --use-case=uc-get-order --angle=unit-test pass # ${{ fixtures.existing-order-id }}"
