# StackComponent example — protocol kind (gRPC).
schema_version: 1.0.0
id: grpc
kind: protocol
name: grpc
version: "1.60"
capabilities:
  - streaming
  - http2-transport
  - protobuf-codec
detection_evidence:
  - file: package.json
    path: dependencies.@grpc/grpc-js
    value: ^1.60.0
